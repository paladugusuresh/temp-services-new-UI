# Environment Variables for Development and Production

# API Base URL for fetching estimates
# For local development with API running on port 8080:
PRICING_API_BASE=http://localhost:8080

# For Azure App Service deployment:
# PRICING_API_BASE=https://your-api-app.azurewebsites.net

# Site Base URL (used in sitemap and robots.txt)
# For local development:
SITE_URL=http://localhost:3000

# For production:
# SITE_URL=https://temp-services.com

# Environment (automatically set by Next.js, but can override)
# NODE_ENV=development
# NODE_ENV=production
